package:
  git:
    installed: true
  mercurial:
    installed: true
  xvfb:
    installed: true
  apt:
    installed: true
  locales:
    installed: true
  sudo:
    installed: true 
  apt-transport-https:
    installed: true
  ca-certificates:
    installed: true
  openssh-client:
    installed: true
  software-properties-common:
    installed: true
  build-essential:
    installed: true
  tar:
    installed: true
  lsb-release:
    installed: true
  gzip:
    installed: true
  parallel:
    installed: true
  net-tools:
    installed: true
  netcat:
    installed: true
  unzip:
    installed: true
  zip:
    installed: true
  bzip2:
    installed: true
  gnupg:
    installed: true
  curl:
    installed: true
  wget:
    installed: true
  build-essential:
    installed: true
  tree:
    installed: true
  google-cloud-sdk:
    installed: true
  libqt5widgets5:
    installed: true
command:
  docker -v:
    exit-status: 0
  docker-compose --version:
    exit-status: 0
  gradle -v:
    exit-status: 0
  env | grep -q ANDROID_HOME:
    exit-status: 0
  sdkmanager --version:
    exit-status: 0
  env | grep -q LD_LIBRARY_PATH:
    exit-status: 0
  sdkmanager --list|grep -q 'build-tools/26.0':
    exit-status: 0
  sdkmanager --list|grep -q 'build-tools/27.0':
    exit-status: 0
  sdkmanager --list|grep -q 'build-tools/28.0':
    exit-status: 0
  sdkmanager --list|grep -q 'emulator':
    exit-status: 0
  sdkmanager --list|grep -q 'patcher':
    exit-status: 0
  sdkmanager --list|grep -q 'platform-tools':
    exit-status: 0
  sdkmanager --list|grep -q 'emulator':
    exit-status: 0
  sdkmanager --list|grep -q 'platforms/android':
    exit-status: 0
